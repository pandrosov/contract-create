# Contract Management System

Система управления договорами с возможностью создания документов из шаблонов.

## 🚀 Возможности

- **Управление пользователями** - регистрация, авторизация, права доступа
- **Управление папками** - создание, удаление, организация структуры
- **Управление шаблонами** - загрузка .docx шаблонов с плейсхолдерами
- **Генерация документов** - создание документов из шаблонов с заполнением полей
- **Безопасность** - CSRF защита, JWT токены, валидация данных

## 🛠 Технологии

### Backend
- **FastAPI** - современный веб-фреймворк для Python
- **SQLAlchemy** - ORM для работы с базой данных
- **PostgreSQL** - реляционная база данных
- **python-docx** - работа с .docx файлами
- **JWT** - аутентификация пользователей

### Frontend
- **React** - библиотека для создания пользовательских интерфейсов
- **Axios** - HTTP клиент для API запросов
- **React Router** - маршрутизация в приложении

### Инфраструктура
- **Docker** - контейнеризация приложения
- **Docker Compose** - оркестрация контейнеров

## 📋 Требования

- Docker
- Docker Compose

## 🚀 Быстрый старт

1. **Клонируйте репозиторий**
```bash
git clone <repository-url>
cd create_document
```

2. **Запустите приложение**
```bash
docker-compose up --build -d
```

3. **Инициализируйте базу данных**
```bash
docker-compose exec backend python init_db.py
docker-compose exec backend python activate_admin.py
```

4. **Откройте приложение**
- Frontend: http://localhost:3000
- Backend API: http://localhost:8000
- API документация: http://localhost:8000/docs

## 👤 Данные для входа

- **Логин**: `admin`
- **Пароль**: `admin`

## 📁 Структура проекта

```
create_document/
├── app/                    # Backend приложение
│   ├── api/               # API endpoints
│   ├── core/              # Основные модули (DB, security)
│   ├── models/            # SQLAlchemy модели
│   ├── schemas/           # Pydantic схемы
│   └── services/          # Бизнес-логика
├── frontend/              # React приложение
│   ├── src/
│   │   ├── api/          # API клиенты
│   │   ├── components/   # React компоненты
│   │   ├── pages/        # Страницы приложения
│   │   └── context/      # React контексты
├── templates/             # Хранилище шаблонов
├── docker-compose.yaml    # Docker Compose конфигурация
└── requirements.txt       # Python зависимости
```

## 🔧 API Endpoints

### Аутентификация
- `POST /auth/register` - регистрация пользователя
- `POST /auth/token` - получение токена
- `GET /auth/me` - информация о текущем пользователе
- `POST /auth/logout` - выход из системы

### Папки
- `GET /folders/` - список папок
- `POST /folders/` - создание папки
- `DELETE /folders/{id}` - удаление папки

### Шаблоны
- `GET /templates/folder/{folder_id}` - шаблоны в папке
- `POST /templates/` - загрузка шаблона
- `DELETE /templates/{id}` - удаление шаблона
- `GET /templates/{id}/fields` - поля шаблона
- `POST /templates/{id}/generate` - генерация документа

## 📝 Использование

### 1. Управление папками
- Создайте папки для организации шаблонов
- Удалите ненужные папки

### 2. Загрузка шаблонов
- Выберите папку для шаблона
- Загрузите .docx файл с плейсхолдерами `{{FIELD_NAME}}`
- Система автоматически извлечет поля из шаблона

### 3. Создание документов
- Перейдите на страницу "Создать документ"
- Выберите папку и шаблон
- Заполните поля формы
- Скачайте сгенерированный документ

## 🔒 Безопасность

- **CSRF защита** - все модифицирующие операции защищены
- **JWT токены** - безопасная аутентификация
- **Валидация данных** - проверка входных данных
- **Права доступа** - административные функции

## 🐛 Устранение неполадок

### Проблемы с авторизацией
```bash
# Пересоздайте администратора
docker-compose exec backend python activate_admin.py
```

### Проблемы с базой данных
```bash
# Переинициализируйте БД
docker-compose exec backend python init_db.py
```

### Проблемы с контейнерами
```bash
# Перезапустите все контейнеры
docker-compose down
docker-compose up --build -d
```

## 🤝 Разработка

### Добавление новых функций
1. Создайте модель в `app/models/`
2. Добавьте схему в `app/schemas/`
3. Создайте сервис в `app/services/`
4. Добавьте API endpoint в `app/api/`
5. Обновите фронтенд компоненты

### Структура шаблонов
Шаблоны должны содержать плейсхолдеры в формате `{{FIELD_NAME}}`:
```
Договор №{{CONTRACT_NUMBER}}
от {{CONTRACT_DATE}}

Заказчик: {{CUSTOMER_NAME}}
Исполнитель: {{CONTRACTOR_NAME}}
```

## 📄 Лицензия

MIT License

## 👥 Авторы

- Разработано для системы управления договорами 